---
/*
  /archives/ 已并入 /posts/：文章页第一页有「卡片 / 时间轴」切换，
  时间轴视图就是原来这个页面在做的事（按年 / 月速查）。

  这里留一个跳转页，避免旧链接和搜索引擎收录的地址 404 —— 和 /music → /collection
  同一套做法。vercel.json 里另外配了一条真正的 301，静态托管下优先走那条。
*/
return Astro.redirect("/posts/", 301);
---
