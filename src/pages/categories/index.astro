---
/*
  栏目总览页已经并入文章页：/posts/ 顶部就是栏目筛选条（含篇数与栏目色），
  点一下就地过滤，没必要再单独开一页把同样六个栏目列一遍。

  这里留一个跳转页，避免旧链接和搜索引擎收录的地址 404 —— 和 /archives、/music 同一套做法；
  vercel.json 里另配了同名 301，静态托管下优先走那条。

  注意：/categories/<栏目>/ 的六个页面仍然保留（无 JS 时的兜底 + 主题落点），
  所以这里的规则是精确匹配 /categories/，不要写成 /categories/:path*。
*/
return Astro.redirect("/posts/", 301);
---
